plugins {
    id 'java'
    id 'org.springframework.boot' version '2.2.2.RELEASE'
}

apply plugin: 'java'
apply plugin: 'io.spring.dependency-management'

group = 'com.sb'
version = '1.1-SNAPSHOT'
sourceCompatibility = JavaVersion.VERSION_11

repositories {
    jcenter()
    maven {
        url "https://artifactory.sergeybochkov.com/artifactory/libs-release"
    }
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-data-mongodb'

    implementation(group: 'resnyx', name: 'resnyx', version: '1.1.1')
    implementation(group: 'com.jcabi', name: 'jcabi-http', version: '1.17.2')
    implementation 'com.sun.jersey:jersey-client:1.19.4'

    compile group: 'org.jsoup', name: 'jsoup', version: '1.12.1'

    compileOnly 'org.projectlombok:lombok:1.18.10'
    annotationProcessor 'org.projectlombok:lombok:1.18.10'

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

bootJar {
    archiveFileName = "resnyx-bot.jar"
}